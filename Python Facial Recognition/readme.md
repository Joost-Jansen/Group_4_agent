**run these scripts:** 

py -3 -m venv .venv

.venv\scripts\activate

python -m pip install --upgrade pip

pip install flask

pip install opencv-contrib-python

pip install dlib

run by:

**python -m flask run**

